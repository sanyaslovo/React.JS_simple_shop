export const selectCartItems = (state) => state.cart.cartItems;
export const descreaseCartItem = (state) => state.cart.cartItems.id;
export const encreaseCartItem = (state) => state.cart.cartItems.id;
export const removeCartItem = (state) => state.cart.cartItems.id;
